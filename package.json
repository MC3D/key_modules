{
  "name": "key_modules",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0"
  },
  "scripts": {
    "browserify": "browserify main.js -o bundle.js -t [ babelify --presets [ es2015 ] ] "
  }
}
